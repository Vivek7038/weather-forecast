function fahrenheitToCelsius(fahrenheit) {
       // Formula for Fahrenheit to Celsius conversion: (°F - 32) * 5/9
       const celsius = (fahrenheit - 32) * (5 / 9);
       return celsius;
     }

     
     export default fahrenheitToCelsius